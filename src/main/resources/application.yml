spring:
  datasource:
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
    username: sa
    password: rjbc2095
    url: jdbc:sqlserver://LAPTOP-S0I071QI\\SQLEXPRESS:1433;databaseName=NorthwindX
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
server:
  port: 8085
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8089/eureka/
logging:
  file:
    name: /home/usrfc/mth-applications/biller/log/northwindx.log
  level:
    root: ERROR
    org:
      springframework: ERROR
      hibernate: ERROR
    com:
      rbenitez:
        lambdas: DEBUG
  pattern:
    console: "%d{dd-MM-yyyy HH:mm:ss.SSS} [%thread] %-5level %logger{36}.%M - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36}.%M - %msg%n"